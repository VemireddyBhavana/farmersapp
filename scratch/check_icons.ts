import * as Fi from 'react-icons/fi';
console.log(Object.keys(Fi).filter(k => k.toLowerCase().includes('drop')));
console.log(Object.keys(Fi).filter(k => k.toLowerCase().includes('target')));
console.log(Object.keys(Fi).filter(k => k.toLowerCase().includes('zap')));
